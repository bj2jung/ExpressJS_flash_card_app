extends layout.pug

block content
    h2= text
    if hint
        p
            i Hint: #{hint}
    a(href=`/card/${cardnumber}?side=${sideToShow}`)= sideToShow
    br
    a(href='/card') Next question
   

